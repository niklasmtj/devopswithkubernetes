apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: image-claim
  namespace: main-app
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi